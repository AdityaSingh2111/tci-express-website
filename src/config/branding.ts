export const brandingConfig = {
  logoSizes: {
    /**
     * Desktop Header Logo
     * Visible on screens 768px and wider.
     * Increase these numbers to make the top-left logo larger on desktop.
     */
    navbarDesktop: {
      width: 240,
      height: 80
    },

    /**
     * Mobile Header Logo
     * Visible on phones and tablets under 768px.
     * Increase these numbers to make the top-left logo larger on mobile.
     */
    navbarMobile: {
      width: 160,
      height: 100
    },

    /**
     * Mobile Slide-out Menu Logo
     * The logo shown inside the side menu when the hamburger icon is clicked.
     */
    mobileMenu: {
      width: 120,
      height: 86
    },

    /**
     * Desktop Footer Logo
     * Visible on screens 768px and wider at the very bottom.
     * Increase these numbers to make the dark-background logo larger.
     */
    footerDesktop: {
      width: 320,
      height: 72
    },

    /**
     * Mobile Footer Logo
     * Visible on phones and tablets under 768px at the very bottom.
     */
    footerMobile: {
      width: 100,
      height: 60
    }
  }
};
